export const MARKET_PICKS_LIMIT = 6;
/** Homepage "Today's picks" — three live movers, not a six-card dummy row. */
export const TODAYS_PICKS_LIMIT = 3;
/** Hero fan slots. */
export const HERO_FAN_SIZE = 5;
/** Shared live pool for hero, marquee, and today's picks. */
export const HOME_LIVE_POOL_LIMIT = 32;
